<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>✧ 𝑨𝑳𝑯𝑾𝑨𝑹𝒀 𝑨𝒊 ✧</title>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;800&family=Orbitron:wght@700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* ═══════════════════════════════════════════
           🌟 Sonic Dynamic Visual Tokens & Variables
           ═══════════════════════════════════════════ */
        :root {
            --sonic-blue: #00d4ff;
            --sonic-dark-blue: #0099cc;
            --royal-gold: #ffd700;
            --dark-bg: #0a0e17;
            --card-bg: rgba(15, 23, 42, 0.85);
            --accent: #7269e3;
            --accent-glow: rgba(114, 105, 227, 0.4);
            --shadow-glow: 0 0 30px rgba(0, 212, 255, 0.4);
            --shadow-gold: 0 0 30px rgba(255, 215, 0, 0.4);
            --gradient-main: linear-gradient(135deg, #00d4ff 0%, #0099cc 50%, #7269e3 100%);
            --gradient-gold: linear-gradient(135deg, #ffd700 0%, #ffaa00 50%, #ffd700 100%);
            --text-color: #c5c6c7;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Cairo', sans-serif;
        }

        html, body {
            height: 100%;
            background-color: var(--dark-bg);
            color: #fff;
            overflow-x: hidden;
        }

        body {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* ═══════════════════════════════════════════
           🌟 Dynamic Particles Background Effect
           ═══════════════════════════════════════════ */
        .particles-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            pointer-events: none;
            z-index: 0;
            overflow: hidden;
        }

        .particle {
            position: absolute;
            width: 5px;
            height: 5px;
            background: var(--sonic-blue);
            border-radius: 50%;
            opacity: 0.6;
            animation: float-particle 15s infinite ease-in-out;
            box-shadow: 0 0 10px var(--sonic-blue);
        }

        .particle.gold {
            background: var(--royal-gold);
            box-shadow: 0 0 10px var(--royal-gold);
            animation-duration: 18s;
        }

        @keyframes float-particle {
            0%, 100% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
            10% { opacity: 0.7; }
            90% { opacity: 0.7; }
            100% { transform: translateY(-100vh) rotate(720deg); opacity: 0; }
        }

        /* ═══════════════════════════════════════════
           📍 Glassmorphic Header & Glow Button
           ═══════════════════════════════════════════ */
        header {
            width: 100%;
            padding: 14px 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(10, 14, 23, 0.85);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0, 212, 255, 0.2);
            position: sticky;
            top: 0;
            z-index: 10;
            transition: all 0.3s ease;
        }

        header.scrolled {
            box-shadow: 0 5px 30px rgba(0, 212, 255, 0.2);
            background: rgba(10, 14, 23, 0.95);
        }

        header h1 {
            font-size: 1.3rem;
            font-family: 'Orbitron', 'Cairo', sans-serif;
            background: var(--gradient-main);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            font-weight: 800;
            letter-spacing: 1px;
            animation: pulse-title 3s infinite;
        }

        @keyframes pulse-title {
            0%, 100% { filter: drop-shadow(0 0 2px rgba(0,212,255,0.2)); }
            50% { filter: drop-shadow(0 0 10px rgba(0,212,255,0.6)); }
        }

        .dots-btn {
            background: transparent;
            border: 1px solid rgba(0, 212, 255, 0.3);
            color: var(--sonic-blue);
            font-size: 1.5rem;
            cursor: pointer;
            padding: 4px 12px;
            line-height: 1;
            border-radius: 8px;
            transition: all 0.3s ease;
            box-shadow: 0 0 10px rgba(0, 212, 255, 0.1);
        }

        .dots-btn:hover {
            color: #fff;
            background: var(--gradient-main);
            border-color: transparent;
            box-shadow: var(--shadow-glow);
            transform: scale(1.05) rotate(90deg);
        }

        /* ═══════════════════════════════════════════
           📱 Sidebar Interactive Slide
           ═══════════════════════════════════════════ */
        .sidebar-overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(5px);
            display: none;
            z-index: 90;
            transition: opacity 0.3s ease;
        }

        .sidebar {
            position: fixed;
            top: 0;
            right: -300px;
            width: 280px;
            height: 100%;
            background: rgba(15, 23, 42, 0.95);
            backdrop-filter: blur(25px);
            border-left: 1px solid rgba(0, 212, 255, 0.2);
            box-shadow: -10px 0 30px rgba(0, 0, 0, 0.8);
            z-index: 100;
            transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            display: flex;
            flex-direction: column;
            padding: 25px 20px;
        }

        .sidebar.active {
            right: 0;
        }

        .sidebar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-bottom: 15px;
            border-bottom: 1px solid rgba(0, 212, 255, 0.2);
            margin-bottom: 20px;
        }

        .sidebar-header h3 {
            font-size: 1.1rem;
            color: var(--sonic-blue);
            font-weight: 700;
        }

        .sidebar-close {
            background: transparent;
            border: none;
            color: #ef4444;
            font-size: 1.4rem;
            cursor: pointer;
            transition: transform 0.2s ease;
        }

        .sidebar-close:hover {
            transform: scale(1.2) rotate(90deg);
        }

        .sidebar-menu {
            display: flex;
            flex-direction: column;
            gap: 12px;
        }

        .menu-item {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(0, 212, 255, 0.15);
            color: #fff;
            padding: 14px 16px;
            border-radius: 12px;
            cursor: pointer;
            font-size: 0.95rem;
            display: flex;
            align-items: center;
            gap: 12px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .menu-item::before {
            content: '';
            position: absolute;
            top: 0; right: 0;
            width: 3px; height: 100%;
            background: var(--gradient-main);
            transform: scaleY(0);
            transition: transform 0.3s ease;
        }

        .menu-item:hover {
            background: rgba(0, 212, 255, 0.1);
            border-color: var(--sonic-blue);
            transform: translateX(-5px);
            box-shadow: 0 5px 20px rgba(0, 212, 255, 0.2);
        }

        .menu-item:hover::before {
            transform: scaleY(1);
        }

        /* ═══════════════════════════════════════════
           💬 Main Chat & Message Interactions
           ═══════════════════════════════════════════ */
        .main-container {
            width: 100%;
            max-width: 900px;
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 16px;
            gap: 12px;
            height: calc(100vh - 65px);
            z-index: 5;
        }

        .chat-container {
            flex: 1;
            background: var(--card-bg);
            border: 1px solid rgba(0, 212, 255, 0.2);
            border-radius: 20px;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 14px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(12px);
            transition: border-color 0.3s ease;
        }

        .chat-container:hover {
            border-color: rgba(0, 212, 255, 0.4);
        }

        .msg {
            padding: 12px 18px;
            border-radius: 16px;
            max-width: 85%;
            line-height: 1.6;
            font-size: 0.95rem;
            word-break: break-word;
            white-space: pre-wrap;
            animation: fadeInUp 0.4s ease forwards;
            position: relative;
        }

        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .user {
            background: var(--gradient-main);
            color: #0a0e17;
            font-weight: 600;
            align-self: flex-end;
            border-bottom-left-radius: 3px;
            box-shadow: var(--shadow-glow);
        }

        .ai {
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 215, 0, 0.2);
            color: var(--text-color);
            align-self: flex-start;
            border-bottom-right-radius: 3px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        /* ═══════════════════════════════════════════
           🎛️ Input Controls & Glowing Action Buttons
           ═══════════════════════════════════════════ */
        .input-area {
            display: flex;
            gap: 10px;
            background: var(--card-bg);
            padding: 8px 12px;
            border-radius: 16px;
            border: 1px solid rgba(0, 212, 255, 0.25);
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }

        .input-area:focus-within {
            border-color: var(--sonic-blue);
            box-shadow: var(--shadow-glow);
        }

        input {
            flex: 1;
            background: transparent;
            border: none;
            outline: none;
            color: #fff;
            padding: 10px;
            font-size: 1rem;
        }

        button#sendBtn {
            background: var(--gradient-main);
            color: #0a0e17;
            border: none;
            padding: 10px 24px;
            border-radius: 12px;
            font-weight: 800;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.95rem;
            box-shadow: var(--shadow-glow);
        }

        button#sendBtn:hover {
            transform: translateY(-2px);
            box-shadow: 0 0 25px rgba(0, 212, 255, 0.8);
        }

        footer {
            padding: 8px;
            font-size: 0.8rem;
            color: rgba(255, 255, 255, 0.5);
            text-align: center;
            letter-spacing: 1px;
        }

        /* ═══════════════════════════════════════════
           ✨ Royal Modals & Popups
           ═══════════════════════════════════════════ */
        .modal-overlay {
            position: fixed;
            top: 0; left: 0;
            width: 100vw; height: 100vh;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(8px);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 120;
        }

        .modal-content {
            background: rgba(15, 23, 42, 0.95);
            border: 1px solid rgba(255, 215, 0, 0.3);
            border-radius: 20px;
            width: 90%;
            max-width: 440px;
            padding: 24px;
            position: relative;
            box-shadow: var(--shadow-gold);
            color: #fff;
            animation: zoomIn 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        @keyframes zoomIn {
            from { opacity: 0; transform: scale(0.85); }
            to { opacity: 1; transform: scale(1); }
        }

        .modal-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid rgba(255, 215, 0, 0.2);
            padding-bottom: 12px;
            margin-bottom: 18px;
        }

        .modal-header h2 {
            font-size: 1.15rem;
            color: var(--royal-gold);
            font-weight: 700;
        }

        .close-btn {
            background: transparent;
            border: none;
            color: #ef4444;
            font-size: 1.3rem;
            cursor: pointer;
            font-weight: bold;
            transition: transform 0.2s ease;
        }

        .close-btn:hover {
            transform: scale(1.2) rotate(90deg);
        }

        .modal-body { font-size: 0.95rem; line-height: 1.6; }
        .modal-section { display: none; }
        .modal-section.active { display: block; }

        .profile-info { display: flex; align-items: center; gap: 14px; margin-bottom: 15px; }
        .profile-info img {
            width: 52px; height: 52px;
            border-radius: 50%;
            border: 2px solid var(--royal-gold);
            box-shadow: var(--shadow-gold);
        }

        .contact-box {
            background: rgba(255, 255, 255, 0.04);
            padding: 12px;
            border-radius: 12px;
            border: 1px solid rgba(0, 212, 255, 0.2);
            margin-top: 12px;
        }

        .contact-box a {
            color: var(--sonic-blue);
            text-decoration: none;
            word-break: break-all;
            font-weight: bold;
        }

        .theme-select {
            width: 100%;
            padding: 10px;
            background: #0a0e17;
            color: #fff;
            border: 1px solid rgba(0, 212, 255, 0.3);
            border-radius: 10px;
            margin-top: 10px;
            outline: none;
        }

        .btn-danger {
            background: linear-gradient(135deg, #ef4444, #dc2626);
            color: #fff;
            border: none;
            padding: 10px 16px;
            border-radius: 10px;
            cursor: pointer;
            width: 100%;
            margin-top: 18px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        .btn-danger:hover {
            box-shadow: 0 0 15px rgba(239, 68, 68, 0.5);
            transform: translateY(-2px);
        }

        /* Scrollbar Visuals */
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: var(--dark-bg); }
        ::-webkit-scrollbar-thumb { background: var(--sonic-dark-blue); border-radius: 3px; }
        ::-webkit-scrollbar-thumb:hover { background: var(--sonic-blue); }

        @media (min-width: 768px) {
            header { padding: 16px 40px; }
            header h1 { font-size: 1.5rem; }
            .main-container { padding: 20px; gap: 15px; }
        }
    </style>
</head>
<body>

    <!-- 🌟 Dynamic Particles Background -->
    <div class="particles-container" id="particles"></div>

    <!-- الهيدر مع زر الثلاث نقط -->
    <header id="navbar">
        <h1>✧ 𝑨𝑳𝑯𝑾𝑨𝑹𝒀 𝑨𝒊 ✧</h1>
        <button class="dots-btn" onclick="toggleSidebar()">⋮</button>
    </header>

    <!-- خلفية تغطية عند فتح القائمة -->
    <div class="sidebar-overlay" id="sidebarOverlay" onclick="closeSidebar()"></div>

    <!-- القائمة الجانبية المنزلقة من اليمين -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <h3>خيارات التطبيق</h3>
            <button class="sidebar-close" onclick="closeSidebar()">✕</button>
        </div>
        <div class="sidebar-menu">
            <div class="menu-item" onclick="openSection('settings')">
                <span>⚙️</span> قسم الإعدادات
            </div>
            <div class="menu-item" onclick="openSection('account')">
                <span>👤</span> قسم الحساب
            </div>
            <div class="menu-item" onclick="openSection('contact')">
                <span>💬</span> التواصل مع المطور
            </div>
        </div>
    </div>

    <!-- حاوية الشات الرئيسية -->
    <div class="main-container">
        <div class="chat-container" id="chat">
            <div class="msg ai">مرحباً بك في <b>𝑨𝑳𝑯𝑾𝑨𝑹𝒀 𝑨𝒊</b>! أنا نموذج ذكاء اصطناعي تم تطويري بواسطة <b>الهواري وعلي ويكا</b>. كيف يمكننا مساعدتك اليوم؟ 🚀</div>
        </div>

        <div class="input-area">
            <input type="text" id="prompt" placeholder="اكتب سؤالك هنا..." onkeydown="if(event.key==='Enter') send()">
            <button id="sendBtn" onclick="send()">إرسال</button>
        </div>

        <footer>⚜️ Developed By El-Hawary & Ali Weka ⚜️</footer>
    </div>

    <!-- النافذة المنبثقة للتفاصيل -->
    <div class="modal-overlay" id="modalOverlay">
        <div class="modal-content">
            <div class="modal-header">
                <h2 id="modalTitle">العنوان</h2>
                <button class="close-btn" onclick="closeModal()">✕</button>
            </div>

            <div class="modal-body">
                <!-- قسم الإعدادات -->
                <div class="modal-section" id="sec-settings">
                    <label>اختر مظهر المحادثة:</label>
                    <select class="theme-select" onchange="changeTheme(this.value)">
                        <option value="dark">الوضع الليلي (الافتراضي)</option>
                        <option value="purple">الوضع البنفسجي الدافئ</option>
                        <option value="blue">الوضع الأزرق المظلم</option>
                    </select>
                    <div style="margin-top: 15px;">
                        <label><input type="checkbox" id="autoScroll" checked> التمرير التلقائي أثناء الكتابة</label>
                    </div>
                </div>

                <!-- قسم الحساب -->
                <div class="modal-section" id="sec-account">
                    <div class="profile-info">
                        <img id="modalUserImg" src="https://via.placeholder.com/48" alt="user">
                        <div>
                            <h3 id="modalUserName">زائر</h3>
                            <span style="font-size:0.75rem; color:#a08b7a;" id="modalUserMail">غير مسجّل</span>
                        </div>
                    </div>
                    <button class="btn-danger" onclick="logout()">تسجيل الخروج</button>
                </div>

                <!-- قسم التواصل مع المطور -->
                <div class="modal-section" id="sec-contact">
                    <p>يمكنك التواصل المباشر مع المطورين للحصول على الدعم الفني أو لتقديم المقترحات:</p>
                    <div class="contact-box">
                        <b>👨‍💻 المطورون:</b> الهواري وعلي ويكا<br>
                        <b>📬 البريد الإلكتروني:</b><br>
                        <a href="mailto:alhwary@gmail.com?subject=تواصل مع مطوري ALHWARY AI">your-email@gmail.com</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // === 1. إنشاء الجسيمات المتحركة (Sonic Particle System) ===
        function createParticles() {
            const container = document.getElementById('particles');
            const particleCount = 45;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = `particle${Math.random() > 0.65 ? ' gold' : ''}`;
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 15 + 's';
                particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
                container.appendChild(particle);
            }
        }

        // === 2. التحكم بالقائمة الجانبية (Sidebar) ===
        function toggleSidebar() {
            document.getElementById('sidebar').classList.add('active');
            document.getElementById('sidebarOverlay').style.display = 'block';
        }

        function closeSidebar() {
            document.getElementById('sidebar').classList.remove('active');
            document.getElementById('sidebarOverlay').style.display = 'none';
        }

        function openSection(section) {
            closeSidebar();
            openModal(section);
        }

        // === 3. التحكم بالنافذة المنبثقة (Modal) ===
        function openModal(section) {
            document.getElementById('modalOverlay').style.display = 'flex';
            document.querySelectorAll('.modal-section').forEach(s => s.classList.remove('active'));

            const userName = localStorage.getItem('user_name') || 'زائر';
            const userPic = localStorage.getItem('user_picture') || 'https://via.placeholder.com/48';

            if(section === 'settings') {
                document.getElementById('modalTitle').innerText = '⚙️ إعدادات التطبيق';
                document.getElementById('sec-settings').classList.add('active');
            } else if(section === 'account') {
                document.getElementById('modalTitle').innerText = '👤 تفاصيل الحساب';
                document.getElementById('sec-account').classList.add('active');
                document.getElementById('modalUserName').innerText = userName;
                document.getElementById('modalUserMail').innerText = userName.includes('@') ? userName : userName + '@gmail.com';
                document.getElementById('modalUserImg').src = userPic;
            } else if(section === 'contact') {
                document.getElementById('modalTitle').innerText = '💬 التواصل مع المطورين';
                document.getElementById('sec-contact').classList.add('active');
            }
        }

        function closeModal() {
            document.getElementById('modalOverlay').style.display = 'none';
        }

        function changeTheme(val) {
            if(val === 'purple') {
                document.documentElement.style.setProperty('--dark-bg', '#120b18');
                document.documentElement.style.setProperty('--card-bg', 'rgba(38, 22, 48, 0.85)');
                document.documentElement.style.setProperty('--sonic-blue', '#a855f7');
            } else if(val === 'blue') {
                document.documentElement.style.setProperty('--dark-bg', '#07111e');
                document.documentElement.style.setProperty('--card-bg', 'rgba(15, 30, 52, 0.85)');
                document.documentElement.style.setProperty('--sonic-blue', '#3b82f6');
            } else {
                document.documentElement.style.setProperty('--dark-bg', '#0a0e17');
                document.documentElement.style.setProperty('--card-bg', 'rgba(15, 23, 42, 0.85)');
                document.documentElement.style.setProperty('--sonic-blue', '#00d4ff');
            }
        }

        function logout() {
            localStorage.clear();
            window.location.href = 'login.html';
        }

        // === 4. تأثيرات التمرير والهيدر ===
        window.addEventListener('scroll', () => {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 30) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // === 5. الاتصال بالذكاء الاصطناعي ===
        const encodedUrl = "aHR0cHM6Ly9hcGkucmV3aW5kLmFpL3YxL2NoYXQvY29tcGxldGlvbnM=";
        const apiUrl = atob(encodedUrl);
        const history = [];

        function typeWriter(element, text, speed = 12, callback) {
            let i = 0;
            element.innerText = "";
            function type() {
                if (i < text.length) {
                    element.innerText += text.charAt(i);
                    i++;
                    const chat = document.getElementById('chat');
                    if(document.getElementById('autoScroll').checked) {
                        chat.scrollTop = chat.scrollHeight;
                    }
                    setTimeout(type, speed);
                } else if (callback) {
                    callback();
                }
            }
            type();
        }

        async function send() {
            const input = document.getElementById('prompt');
            const sendBtn = document.getElementById('sendBtn');
            const chat = document.getElementById('chat');
            const text = input.value.trim();

            if (!text) return;

            input.disabled = true;
            sendBtn.disabled = true;

            chat.innerHTML += `<div class="msg user">${text}</div>`;

            let apiContent = text;
            if (history.length === 0) {
                apiContent = `[توجيه خفي: أنت ذكاء اصطناعي اسمك ALHWARY AI تم تطويرك بواسطة (الهواري وعلي ويكا) فقط. إذا تم سؤالك عن هويتك أو مطوريك أجب بذلك بوضوح ولا تذكر أي شركات أخرى].\n\nالسؤال: ${text}`;
            }

            history.push({ role: "user", content: apiContent });
            input.value = '';
            chat.scrollTop = chat.scrollHeight;

            const loading = document.createElement('div');
            loading.className = 'msg ai';
            loading.innerText = '⏳ جاري التفكير...';
            chat.appendChild(loading);

            try {
                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        model: "openai/gpt-5-nano",
                        messages: history,
                        temperature: 0.7,
                        max_tokens: 16000,
                        stream: false
                    })
                });

                const data = await response.json();
                const reply = data?.choices?.[0]?.message?.content || "لم يتم الحصول على إجابة.";

                typeWriter(loading, reply, 12, () => {
                    input.disabled = false;
                    sendBtn.disabled = false;
                    input.focus();
                });

                history.push({ role: "assistant", content: reply });

            } catch (err) {
                loading.innerText = "❌ حدث خطأ أثناء الاتصال بالذكاء الاصطناعي.";
                input.disabled = false;
                sendBtn.disabled = false;
            }
        }

        // تشغيل المؤثرات عند فتح الصفحة
        document.addEventListener('DOMContentLoaded', () => {
            createParticles();
        });
    </script>
</body>
</html>
